<h1>Add ingredients for your <%= @cocktail.name %> cocktail</h1>

<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.association :ingredient, collection: @ingredients %>
  <%= f.input :description %>
  <%= f.submit %>
<% end %>
